# SPDX-FileCopyrightText: 2025 Forkbomb BV
#
# SPDX-License-Identifier: AGPL-3.0-or-later

name: "SEcond 👺 Lint PR title"
on:
  pull_request:

permissions:
  pull-requests: read
  contents: read
  
jobs:
  main:
    runs-on: ubuntu-latest
    secrets: inherit
    permissions:
      pull-requests: read
    steps:
      - uses: amannn/action-semantic-pull-request@v5
