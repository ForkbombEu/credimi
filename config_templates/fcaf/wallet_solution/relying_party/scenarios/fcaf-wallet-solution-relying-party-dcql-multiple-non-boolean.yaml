name: 'FCAF ProtocolMessages 072: non-boolean DCQL multiple'
runtime:

steps:
- id: create
  use: http-request
  with:
    method: POST
    url: ${fixture.verifier_url}/openid4vp/sessions
    expected_status: 201
    headers: { Content-Type: application/json }
    body:
      request_uri_method: post
      response_mode: direct_post.jwt
      presentation_request:
        dcql_query:
          credentials:
          - id: pid
            format: dc+sd-jwt
            multiple: not-a-boolean
            meta: { vct_values: ["urn:eudi:pid:1"] }
            claims: [{ path: [given_name] }]
- id: wallet
  use: mobile-automation
  with:
    action_id: forkbomb-bv-andrea/eudiw-beta-wallet/fcaf-expect-request-rejected
    version_id: installed_from_external_source
    parameters:
      deeplink: "${{ create.outputs.body.deeplink }}"
- id: session
  use: http-request
  with: { method: GET, url: '${fixture.verifier_url}/openid4vp/sessions/${{ create.outputs.body.session_id }}', expected_status: 200 }
- id: request
  use: http-request
  with: { method: GET, url: '${fixture.verifier_url}/openid4vp/sessions/${{ create.outputs.body.session_id }}/request', expected_status: 200 }
- id: run-fcaf-validators
  use: fcaf-validation
  with:
    test_ids: [WS_RP_MS_ProtocolMessages__072]
    suite: wallet_solution/relying_party
    pipeline_outputs:
      pipeline.dcql.multiple-non-boolean:
        output:
          request_object: ${{ request.outputs.body }}
          dcql_exchange: ${{ session.outputs.body }}
          visual_evidence: ${{ wallet.outputs.flow_output.maestro_screenshot_urls }}
