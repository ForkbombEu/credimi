name: FCAF default x509_hash metadata
runtime:
  temporal:
    activity_options:
      schedule_to_close_timeout: 50m
      start_to_close_timeout: 50m
      retry_policy:
        maximum_attempts: 1
steps:
  - id: create-presentation
    use: http-request
    with:
      method: POST
      url: ${fixture.verifier_url}/openid4vp/sessions
      expected_status: 201
      headers:
        Content-Type: application/json
      body:
        scheme: openid4vp://
        request_delivery: by_reference
        request_uri_method: get
        response_type: vp_token
        response_mode: direct_post.jwt
        presentation_request:
          nonce: fcaf-metadata-default-x509-hash
          dcql_query:
            credentials:
              - id: pid_mdoc
                format: mso_mdoc
                meta:
                  doctype_value: eu.europa.ec.eudi.pid.1
                claims:
                  - path: [eu.europa.ec.eudi.pid.1, given_name]
  - id: exercise-wallet
    use: mobile-automation
    with:
      action_id: forkbomb-bv-andrea/eudiw-beta-wallet/fcaf-exercise-wallet-generic
      version_id: installed_from_external_source
      parameters:
        deeplink: ${{ create-presentation.outputs.body.deeplink }}
  - id: fetch-capture-session
    use: http-request
    with:
      method: GET
      url: ${fixture.verifier_url}/openid4vp/sessions/${{ create-presentation.outputs.body.session_id }}
      expected_status: 200
  - id: run-fcaf-validators
    use: fcaf-validation
    with:
      test_ids: [WS_RP_MS_Metadata__129, WS_RP_MS_Metadata__131, WS_RP_SM_RpIntegrity__029, WS_RP_SM_RpIntegrity__031]
      suite: wallet_solution/relying_party
      pipeline_outputs:
        pipeline.wallet.metadata.default-x509-hash:
          output:
            signed_request_object: ${{ fetch-capture-session.outputs.body.raw.authorization_request_jwt }}
            capture_session: ${{ fetch-capture-session.outputs.body }}
            visual_evidence: ${{ exercise-wallet.outputs.flow_output.maestro_screenshot_urls }}
