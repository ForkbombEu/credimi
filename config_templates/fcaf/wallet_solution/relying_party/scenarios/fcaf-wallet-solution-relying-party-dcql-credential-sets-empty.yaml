name: FCAF DCQL credential sets empty
runtime:

steps:
  - id: create
    use: http-request
    with:
      method: POST
      url: ${fixture.verifier_url}/openid4vp/sessions
      expected_status: 201
      headers: { Content-Type: application/json }
      body: { presentation_request: { dcql_query: { credentials: [{ id: pid, format: dc+sd-jwt, meta: { vct_values: ['urn:eudi:pid:1'] } }], credential_sets: [] } } }
  - id: reject
    use: mobile-automation
    with:
      action_id: forkbomb-bv-andrea/eudiw-beta-wallet/fcaf-expect-request-rejected
      version_id: installed_from_external_source
      parameters:
        deeplink: "${{ create.outputs.body.deeplink }}"
  - id: fetch
    use: http-request
    with: { method: GET, url: "${fixture.verifier_url}/openid4vp/sessions/${{ create.outputs.body.session_id }}", expected_status: 200 }
  - id: validate
    use: fcaf-validation
    with:
      test_ids: [WS_RP_MS_ProtocolMessages__058]
      suite: wallet_solution/relying_party
      pipeline_outputs:
        pipeline.dcql.credential-sets-empty:
          output:
            dcql_exchange: ${{ fetch.outputs.body }}
            visual_evidence: ${{ reject.outputs.flow_output.maestro_screenshot_urls }}
