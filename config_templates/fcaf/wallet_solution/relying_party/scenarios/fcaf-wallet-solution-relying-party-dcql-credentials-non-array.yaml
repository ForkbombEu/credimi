name: FCAF DCQL credentials non-array
runtime:

steps:
  - id: create-capture-session
    use: http-request
    with:
      method: POST
      url: ${fixture.verifier_url}/openid4vp/sessions
      expected_status: 201
      headers: { Content-Type: application/json }
      body: { presentation_request: { dcql_query: { credentials: {} } } }
  - id: reject-non-array-credentials
    use: mobile-automation
    with:
      action_id: forkbomb-bv-andrea/eudiw-beta-wallet/fcaf-expect-request-rejected
      version_id: installed_from_external_source
      parameters:
        deeplink: "${{ create-capture-session.outputs.body.deeplink }}"
  - id: fetch-capture-session
    use: http-request
    with:
      method: GET
      url: "${fixture.verifier_url}/openid4vp/sessions/${{ create-capture-session.outputs.body.session_id }}"
      expected_status: 200
  - id: run-fcaf-validators
    use: fcaf-validation
    with:
      test_ids: [WS_RP_MS_ProtocolMessages__056]
      suite: wallet_solution/relying_party
      pipeline_outputs:
        pipeline.dcql.credentials-non-array:
          output:
            dcql_exchange: ${{ fetch-capture-session.outputs.body }}
            visual_evidence: ${{ reject-non-array-credentials.outputs.flow_output.maestro_screenshot_urls }}
