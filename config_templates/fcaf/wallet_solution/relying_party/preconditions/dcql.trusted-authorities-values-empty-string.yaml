id: pipeline.dcql.trusted-authorities-values-empty-string
kind: pipeline
description: Reject trusted_authorities.values arrays containing an empty string.
pipeline_id: forkbomb-bv-andrea/fcaf-wallet-relying-party-dcql-trusted-authorities-values-empty-string
required_steps:
- exercise-trusted-authorities-values-empty-string
outputs:
  single_value:
    path: $.output.single_value
    decoder: raw
  mixed_value:
    path: $.output.mixed_value
    decoder: raw
  visual_evidence:
    path: $.output.exercise-trusted-authorities-values-empty-string.flow_output.maestro_screenshot_urls
    decoder: raw
failure_policy: fail_dependent_preconditions_and_tests
