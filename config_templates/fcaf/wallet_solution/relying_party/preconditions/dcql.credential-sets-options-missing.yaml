id: pipeline.dcql.credential-sets-options-missing
kind: pipeline
description: Reject a DCQL request whose credential_sets entry omits options.
pipeline_id: forkbomb-bv-andrea/fcaf-wallet-solution-relying-party-dcql-credential-sets-options-missing
required_steps:
  - reject-missing-credential-sets-options
  - fetch-result
outputs:
  dcql_exchange: { path: $.output.fetch-result.outputs.body, decoder: raw }
  visual_evidence: { path: $.output.reject-missing-credential-sets-options.outputs.screenshot_urls, decoder: raw }
failure_policy: fail_dependent_preconditions_and_tests
