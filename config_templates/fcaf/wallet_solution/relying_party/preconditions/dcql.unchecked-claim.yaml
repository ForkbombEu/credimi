id: pipeline.dcql.unchecked-claim
kind: pipeline
description: Request given_name and address, then prove the user-unchecked address is absent from the SD-JWT response.
pipeline_id: forkbomb-bv-andrea/fcaf-dcql-unchecked-claim-is-omitted
required_steps:
- present-with-unchecked-address
- fetch-result
outputs:
  dcql_exchange:
    path: $.output.fetch-result.body
    decoder: raw
  visual_evidence:
    path: $.output.present-with-unchecked-address.flow_output.maestro_screenshot_urls
    decoder: raw
