id: pipeline.dcql.claim-sets-partial-fallback
kind: pipeline
description: Skip a partially satisfiable claim set and present the next PID claim set.
pipeline_id: forkbomb-bv-andrea/fcaf-wallet-solution-relying-party-dcql-claim-sets-partial-fallback
required_steps:
  - present-partial-fallback
  - fetch-result
outputs:
  dcql_exchange:
    path: $.output.fetch-result.body
    decoder: raw
  visual_evidence:
    path: $.output.present-partial-fallback.flow_output.maestro_screenshot_urls
    decoder: raw
failure_policy: fail_dependent_preconditions_and_tests
