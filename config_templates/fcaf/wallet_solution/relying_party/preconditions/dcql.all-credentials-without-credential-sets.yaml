id: pipeline.dcql.all-credentials-without-credential-sets
kind: pipeline
description: Present both requested PID query objects as one required bundle when credential_sets is omitted.
pipeline_id: forkbomb-bv-andrea/fcaf-wallet-solution-relying-party-dcql-all-credentials-without-credential-sets
required_steps: [present-all-credentials, fetch-result]
outputs: { dcql_exchange: { path: $.output.fetch-result.outputs.body, decoder: raw }, visual_evidence: { path: $.output.present-all-credentials.outputs.screenshot_urls, decoder: raw } }
