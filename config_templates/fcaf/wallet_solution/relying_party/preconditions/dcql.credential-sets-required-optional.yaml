id: pipeline.dcql.credential-sets-required-optional
kind: pipeline
description: Present an available required PID query while an unavailable query is optional.
pipeline_id: forkbomb-bv-andrea/fcaf-wallet-solution-relying-party-dcql-credential-sets-required-optional
required_steps:
  - present-required-option
  - fetch-result
outputs:
  dcql_exchange:
    path: $.output.fetch-result.body
    decoder: raw
  visual_evidence:
    path: $.output.present-required-option.flow_output.maestro_screenshot_urls
    decoder: raw
failure_policy: fail_dependent_preconditions_and_tests
