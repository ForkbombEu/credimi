id: pipeline.dcql.credential-sets-options-empty
kind: pipeline
description: Reject an empty credential_sets options array.
pipeline_id: forkbomb-bv-andrea/fcaf-wallet-solution-relying-party-dcql-credential-sets-options-empty
required_steps: [reject-empty-credential-sets-options, fetch-capture-session]
outputs:
  dcql_exchange: { path: $.output.fetch-capture-session.outputs.body, decoder: raw }
  visual_evidence: { path: $.output.reject-empty-credential-sets-options.outputs.screenshot_urls, decoder: raw }
