id: WS_RP_MS_ProtocolMessages__087
title: Handle multiple credential queries matching one credential.
source: { path: MessageStructure/ProtocolMessages/WS_RP_MS_ProtocolMessages__087.md }
suite: { sut: wallet_solution, role: relying_party, section: message_structure.protocol_messages }
applicability: { profile: EUDI_generic, presentation_flow: redirects }
normative_references:
  - { title: OpenID for Verifiable Presentations 1.0, section: "6.1", url: https://openid.net/specs/openid-4-verifiable-presentations-1_0.html }
preconditions:
  - { ref: pipeline.dcql.same-credential-multiple-queries }
evidence:
  dcql_exchange: { from: pipeline.dcql.same-credential-multiple-queries.outputs.dcql_exchange }
  visual_evidence: { from: pipeline.dcql.same-credential-multiple-queries.outputs.visual_evidence }
assertions:
  - id: both_queries_are_satisfied_by_presentations
    validator: dcql.response_satisfies_constraints
    input: evidence.dcql_exchange
    params: { mode: credentials_match }
  - { id: one_wallet_consent_flow_is_evidenced, validator: evidence.non_empty, input: evidence.visual_evidence }
verdict: { pass_when: all_assertions_pass }
