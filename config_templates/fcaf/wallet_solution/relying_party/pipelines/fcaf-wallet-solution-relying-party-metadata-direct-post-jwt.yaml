name: FCAF wallet relying-party direct_post.jwt metadata
runtime:
  global_runner_id: ""
  temporal:
    activity_options:
      schedule_to_close_timeout: 50m
      start_to_close_timeout: 50m
      retry_policy:
        maximum_attempts: 1

steps:
  - id: create-presentation
    use: http-request
    with:
      method: POST
      url: https://verifier-backend.eudiw.dev/ui/presentations
      expected_status: 200
      headers:
        Content-Type: application/json
      body:
        type: vp_token
        nonce: fcaf-metadata-direct-post-jwt
        responseMode: direct_post.jwt
        dcql_query:
          credentials:
            - id: pid_mdoc
              format: mso_mdoc
              meta:
                doctype_value: eu.europa.ec.eudi.pid.1
              claims:
                - path:
                    - eu.europa.ec.eudi.pid.1
                    - given_name
                - path:
                    - eu.europa.ec.eudi.pid.1
                    - family_name

  - id: submit-direct-post-jwt
    use: mobile-automation
    with:
      version_id: installed_from_external_source
      parameters:
        CLIENT_ID: ${{ create-presentation.outputs.body.client_id }}
        REQUEST_URI: ${{ create-presentation.outputs.body.request_uri }}
      action_code: |
        appId: eu.europa.ec.euidi
        ---
        - stopApp
        - evalScript: ${output.authorizationRequest = 'haip-vp://?client_id=' + encodeURIComponent(env.CLIENT_ID) + '&request_uri=' + encodeURIComponent(env.REQUEST_URI) + '&request_uri_method=get'}
        - openLink:
            link: ${output.authorizationRequest}
            autoVerify: false
            browser: false
        - waitForAnimationToEnd:
            timeout: 5000
        - tapOn:
            text: "EUDI Wallet"
            optional: true
        - tapOn:
            text: "Just once"
            optional: true
        - extendedWaitUntil:
            visible: "Welcome back|Share|Cancel|Close"
            timeout: 100000
        - runFlow:
            when:
              visible: "Welcome back"
            commands:
              - tapOn:
                  point: "11%,40%"
              - inputText: "123456"
        - extendedWaitUntil:
            visible: "Share"
            timeout: 30000
        - tapOn:
            text: "Share"
            enabled: true
        - extendedWaitUntil:
            visible: "verify your identity|PIN"
            timeout: 30000
        - inputText: "123456"
        - extendedWaitUntil:
            visible: "Close"
            timeout: 30000
        - assertVisible: "Close"
        - takeScreenshot: fcaf-metadata-direct-post-jwt-submitted

  - id: get-presentation-result
    use: http-request
    with:
      method: GET
      url: https://verifier-backend.eudiw.dev/ui/presentations/${{ create-presentation.outputs.body.transaction_id }}
      expected_status: 200
