# SPDX-FileCopyrightText: 2026 Forkbomb BV
#
# SPDX-License-Identifier: AGPL-3.0-or-later

version: "1.1"
name: WEBUILD flow
env:
  WEBUILD_API: https://webuild.api.forkbomb.eu
tests:
  VCI001:
    steps:
      - name: "Test Case VCI-001: WE BUILD - Authorization code flow - sdjwt - x509 signature"
        http:
          url: ${{ env.WEBUILD_API }}/offer-code-sd-jwt
          method: GET
          params:
            credentialType: urn:eu.europa.ec.eudi:pid:1
            sessionId: [[.session_id | toString]]
            signatureType: x509
          check:
            status: 200
          captures:
            qr:
              jsonpath: $.qr
            deeplink:
              jsonpath: $.deepLink
            session_id:
              jsonpath: $.sessionId
